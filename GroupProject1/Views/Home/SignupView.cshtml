@{
    string[] Times = {"8:00 AM", "9:00 AM", "10:00 AM", "11:00 AM", "12:00 PM", "1:00 PM", "2:00 PM",
        "3:00 PM", "4:00 PM", "5:00 PM", "6:00 PM", "7:00 PM", "8:00 PM" };
}

@section scripts {
    <script>
        $(document).ready(function () {
            $('#dataTable').DataTable({
                "pageLength": 13,
                "paging": true,
                "lengthChange": false,
                "searching": true,
                "ordering": false,
                "info": true,
                "autoWidth": false
            });
        });
    </script>
}

<!--Including some additional libraries for paginated table-->
<link href="https://cdn.datatables.net/1.10.18/css/dataTables.bootstrap4.min.css" rel="stylesheet">

<!--One week worth of time slots hardcoded-->
<div class="container">
    <table class="table table-hover" id="dataTable">
        <thead>

            <tr>
                <th>Date</th>
                <th>Time</th>
                <th>Available Slot</th>
                <th></th>
            </tr>
        </thead>

        <tbody>
            @foreach (var time in Times)
            {
                <tr>
                    <td>4/22/2021</td>
                    <td>@time</td>
                    <td>@time</td>
                    <td>
                        <form asp-action="SignupForm" method="post">
                            <input id="Date" name="Date" type="hidden" value="4/22/2021" />
                            <input id="Time" name="Time" type="hidden" value="@time" />
                            <button type="submit" class="btn btn-primary">Sign Up</button>
                        </form>
                    </td>
                </tr>
            }

            @foreach (var time in Times)
            {
                <tr>
                    <td>4/23/2021</td>
                    <td>@time</td>
                    <td>@time</td>
                    <td>
                        <form asp-action="SignupForm" method="post">
                            <input id="Date" name="Date" type="hidden" value="4/23/2021" />
                            <input id="Time" name="Time" type="hidden" value="@time" />
                            <button type="submit" class="btn btn-primary">Sign Up</button>
                        </form>
                    </td>
                </tr>
            }

            @foreach (var time in Times)
            {
                <tr>
                    <td>4/24/2021</td>
                    <td>@time</td>
                    <td>@time</td>
                    <td>
                        <form asp-action="SignupForm" method="post">
                            <input id="Date" name="Date" type="hidden" value="4/24/2021" />
                            <input id="Time" name="Time" type="hidden" value="@time" />
                            <button type="submit" class="btn btn-primary">Sign Up</button>
                        </form>
                    </td>
                </tr>
            }

            @foreach (var time in Times)
            {
                <tr>
                    <td>4/25/2021</td>
                    <td>@time</td>
                    <td>@time</td>
                    <td>
                        <form asp-action="SignupForm" method="post">
                            <input id="Date" name="Date" type="hidden" value="4/25/2021" />
                            <input id="Time" name="Time" type="hidden" value="@time" />
                            <button type="submit" class="btn btn-primary">Sign Up</button>
                        </form>
                    </td>
                </tr>
            }

            @foreach (var time in Times)
            {
                <tr>
                    <td>4/26/2021</td>
                    <td>@time</td>
                    <td>@time</td>
                    <td>
                        <form asp-action="SignupForm" method="post">
                            <input id="Date" name="Date" type="hidden" value="4/26/2021" />
                            <input id="Time" name="Time" type="hidden" value="@time" />
                            <button type="submit" class="btn btn-primary">Sign Up</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
